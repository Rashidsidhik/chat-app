"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
describe("ServerPingKeeper", () => {
    test("should throw error for interval less than 5 minutes", () => {
        const options = {
            url: "http://localhost:3000",
            intervalMinutes: 4,
        };
        expect(() => new __1.SpinUp(options)).toThrow();
    });
    test("should create instance with valid interval", () => {
        const options = {
            url: "http://localhost:3000",
            intervalMinutes: 15,
        };
        const keeper = new __1.SpinUp(options);
        expect(keeper).toBeInstanceOf(__1.SpinUp);
    });
});
